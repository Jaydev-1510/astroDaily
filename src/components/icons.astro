---
const { glyph, ...props } = Astro.props;
const icons = import.meta.glob<{ default: any }>("../icons/*.svg",{ eager: true });
const Icon = icons[`../icons/${glyph}.svg` as keyof typeof icons]?.default;
---
{Icon && <Icon {...props} />}
